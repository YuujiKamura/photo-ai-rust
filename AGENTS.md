# Agent Rules

- After any Rust code change (including Cargo files), run `cargo build` before reporting progress.
- Keep the local branch synced with the remote frequently (pull/rebase when behind; push when ahead).

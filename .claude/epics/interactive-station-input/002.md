---
name: stationサブコマンド実装
status: open
created: 2026-01-18T00:00:28Z
updated: 2026-01-18T00:00:28Z
github:
depends_on: [001]
parallel: false
conflicts_with: []
---

# Task: stationサブコマンド実装

## Description

`photo-ai-rust station <input.json>` コマンドを追加。解析結果JSONを読み込み、測点が空の写真について対話的に測点を入力できるようにする。

## Acceptance Criteria

- [ ] `station`サブコマンドがCLIに追加される
- [ ] JSONファイルを読み込んで測点が空の写真を抽出
- [ ] 各写真について対話で測点を入力
- [ ] 結果を同じJSONファイルに保存（または別ファイル出力オプション）
- [ ] Ctrl+Cで中断しても途中結果が保存される

## Technical Details

```rust
// src/cli.rs
#[derive(Subcommand)]
enum Commands {
    // 既存...

    /// 対話的に測点を入力
    Station {
        /// 解析結果JSONファイル
        input: PathBuf,

        /// 出力先（省略時は上書き）
        #[arg(short, long)]
        output: Option<PathBuf>,
    },
}
```

## Files to Modify

- `src/cli.rs` - コマンド定義追加
- `src/main.rs` - ハンドラ追加
- `src/station.rs` (新規) - 対話ロジック

## Estimated Effort

1.5時間

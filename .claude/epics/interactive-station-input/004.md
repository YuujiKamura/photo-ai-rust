---
name: 一括操作オプション
status: open
created: 2026-01-18T00:00:28Z
updated: 2026-01-18T00:00:28Z
github:
depends_on: [002]
parallel: true
conflicts_with: [003]
---

# Task: 一括操作オプション

## Description

対話中のショートカットオプションを追加。「前と同じ」「残り全部スキップ」などで操作を効率化。

## Acceptance Criteria

- [ ] `s` - スキップ（この写真の測点を空のまま）
- [ ] `S` - 残り全部スキップ
- [ ] `r` - 前と同じ測点を適用
- [ ] `R` - 残り全部に前と同じ測点を適用
- [ ] `q` - 保存して終了

## Technical Details

```rust
enum StationAction {
    Input(String),      // 測点を入力
    Skip,               // この写真をスキップ
    SkipAll,            // 残り全部スキップ
    Repeat,             // 前と同じ
    RepeatAll,          // 残り全部に前と同じ
    Quit,               // 保存して終了
}

fn prompt_station(photo: &AnalysisResult, prev: Option<&str>) -> Result<StationAction> {
    println!("写真: {} ({}/{})", photo.file_name, current, total);
    println!("[s]スキップ [r]前と同じ [q]終了 または測点入力:");
    // ...
}
```

## Files to Modify

- `src/station.rs` - 一括操作追加

## Estimated Effort

45分
